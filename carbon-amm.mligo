// exchange rate takes into account token supply. "Zone mint entrypoint"


// Weights should be in storage
// How could metadata be updated? And who votes to make that happen? (Gov token holders eventually?)
// Each transaction should shift the weights very slightly. Over time, peoples' preferences
//   will influence the weights. More desireable projects ... should they get cheaper or more expensive?
//   as demand increases, so does price. It should go up in price as demand increases. The more it's traded, 
//   the higher the price. Weights ... that's a vector. The vector will shift
// What about things that 